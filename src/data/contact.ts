interface Contact {
    heading: string;
    description?: string;
}

const contact: Contact = {
    heading: 'Submit a help request and we\'ll get in touch shortly.',
    description: "",
}

export default contact;